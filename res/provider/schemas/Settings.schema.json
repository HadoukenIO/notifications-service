{
    "description": "Shape of the internal settings stored in the database.",
    "type": "object",
    "properties": {
        "id": {
            "description": "List of available settings in the settings collection.",
            "type": "string",
            "enum": [
                "windowVisible"
            ]
        },
        "value": {}
    },
    "$schema": "http://json-schema.org/draft-07/schema#"
}